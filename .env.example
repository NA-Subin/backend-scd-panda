PGHOST=localhost
PGPORT=5432
PGDATABASE=scd_panda
PGSCHEMA=public
PGUSER=postgres
PGPASSWORD=changeme
JWT_SECRET=changeme
PORT=4000
# Comma-separated list of allowed frontend origins
CORS_ORIGIN=http://localhost:3000,http://localhost:3001
